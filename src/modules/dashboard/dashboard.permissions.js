/** Canonical dashboard permission codes shared by routes and permission definitions. */
const dashboardPermissions = Object.freeze({
  read: 'dashboard.read',
  financial: 'dashboard.read.financial',
});

export default dashboardPermissions;
