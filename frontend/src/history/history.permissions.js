/** Dedicated permissions for the three consolidated history areas. */
const historyPermissions = Object.freeze({
  fleet: 'history.fleet.read',
  maintenance: 'history.maintenance.read',
  administration: 'history.administration.read',
});

export default historyPermissions;
